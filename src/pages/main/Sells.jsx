import React from 'react';
import { Box } from '@mui/material';
import { SideBar } from '../../components/SideBar';

export const Sells = () => {
  return (
    <div>
      <Box sx={{ display: 'flex' }}>
        <SideBar />
      </Box>
    </div>
  );
};
